---

all:

  hosts:

    node-1:
      ansible_connection: local
      ansible_host: localhost
      manage_firewall: no
    node-2:
      ansible_connection: ssh
      ansible_ssh_host: 
      manage_firewall: no

  children:
      
    proxy_node:
      hosts:
        node-1:

    remote_nodes:
      hosts:
        node-2:
